services:
  sovia:
    image: saschatpm/sovia:1.0.0
    container_name: sovia
    restart: always
    environment:
      - HTTP_PROXY=${HTTP_PROXY}
      - HTTPS_PROXY=${HTTPS_PROXY}
    network_mode: host
    volumes:
      - db:/data
volumes:
  db:
